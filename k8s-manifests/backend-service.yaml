apiVersion: v1
kind: Service
metadata:
  name: mern-backend-service
  namespace: default
spec:
  type: LoadBalancer
  selector:
    app: mern-backend
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 3000
